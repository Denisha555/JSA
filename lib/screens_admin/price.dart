import 'package:flutter/material.dart';

class HalamanPrice extends StatelessWidget {
  const HalamanPrice({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Price"),
      ),
      body: Center(child: Text("Halaman Price"),),
    );
  }
}