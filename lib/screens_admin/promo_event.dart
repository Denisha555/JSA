import 'package:flutter/material.dart';


class HalamanPromoEvent extends StatelessWidget {
  const HalamanPromoEvent({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Promo & Event"),),
      body: Center(
        child: Text("Halaman Promo & Event"),
      ),
    );
  }
}

