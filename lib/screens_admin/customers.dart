import 'package:flutter/material.dart';

class HalamanCustomers extends StatelessWidget {
  const HalamanCustomers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}